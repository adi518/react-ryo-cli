{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-build": "file:.yalc/react-build",
    "react-dom": "^16.8.6"
  },
  "scripts": {
    "postinstall": "yalc add react-build && yarn --ignore-scripts",
    "start": "react-build start",
    "build": "react-build build",
    "build:production": "react-build build:production",
    "build:stats": "react-build build:stats",
    "test": "react-build test",
    "test:production": "react-build test:production",
    "eject": "react-build eject",
    "inspect": "react-build build --inspect",
    "inspect:output": "react-build build --inspect > output.json",
    "foo": "react-build foo"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:4000"
}
