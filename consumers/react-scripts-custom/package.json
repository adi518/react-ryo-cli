{
  "name": "react-scripts-custom",
  "version": "1.0.0",
  "main": "index.js",
  "bin": "index.js",
  "license": "MIT",
  "scripts": {
    "push": "yalc publish --push",
    "watch": "watch \"yarn run push\" -s ./ --ignoreDirectoryPattern /node_modules/"
  },
  "dependencies": {
    "react-ryo-cli": "file:.yalc/react-ryo-cli"
  },
  "devDependencies": {
    "eslint": "^6.8.0"
  }
}
